
const styles = theme => ({
  root: {
    flexGrow: 1,
    maxWidth: 1080,
    margin: [[0, 'auto']],
    padding: theme.spacing.unit
  },
  actions: {
    position: 'relative',
    zIndex: 1
  }
})

export default styles
