
const styles = theme => ({
  item: {
    color: theme.palette.grey['900'],
    fontWeight: 500,
    fontSize: '1rem'
  },
  itemActive: {
    color: theme.palette.primary.main,
    fontWeight: 600
  }
})

export default styles
